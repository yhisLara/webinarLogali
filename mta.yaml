_schema-version: "3.1"
ID: prodwb
description: A simple CAP project.
version: 1.0.0
modules:
- name: prodwb-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: prodwb-db
  - name: prodwb-auth
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: npm
- name: prodwb-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: prodwb-db
  parameters:
    buildpack: nodejs_buildpack
- name: prodwb-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: prodwb-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - pruebaprodrep.zip
      name: pruebaprodrep
      target-path: resources/
- name: pruebaprodrep
  type: html5
  path: app/prodrep
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: prodwb-db
  type: com.sap.xs.hdi-container
  parameters:
    config:
      schema: WB
    service: hana
    service-plan: hdi-shared
- name: prodwb-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: prodwb-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: prodwb-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: prodwb-html5-srv
    service-plan: app-host
- name: prodwb-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: prodwb-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npx cds build --production
